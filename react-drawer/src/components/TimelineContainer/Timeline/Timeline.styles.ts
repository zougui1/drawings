import { makeStyles } from '../../../utils';

export const useStyles = makeStyles({
  root: {
    width: '100%',
    height: 4,
    backgroundColor: '#fff',
  },
  timelineItems: {
    marginTop: 12,
    display: 'flex',
    position: 'relative',
  },
});
