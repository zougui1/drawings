import { makeStyles } from '../../../utils';

export const useStyles = makeStyles({
  root: {
    position: 'absolute',
    zIndex: 9999999,
  },
  divider: {
    backgroundColor: '#ddd6',
    margin: '6px 0'
  },
});
