import { makeStyles } from '../../../../../utils';

export const useStyles = makeStyles({
  root: {
    display: 'inline-block',
    cursor: 'default',
  },
  elementName: {
    color: '#46afe3',
  },
  pathElementAttribute: {
    display: 'inline-block',
    marginLeft: 25,
  },
});
