import { makeStyles } from '../../../../../../../utils';

export const useStyles = makeStyles({
  root: {
    padding: '2px 2px 0 1px',
    border: `4px solid transparent`,
  },
  hasColor: {
    boxShadow: '0 0 1px #fff6 inset, 0 0 1px #fff6',
  },
});
