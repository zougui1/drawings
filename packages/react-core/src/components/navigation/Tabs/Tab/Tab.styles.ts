import { makeStyles } from '../../../../utils';

export const useStyles = makeStyles({
  root: {
    height: 30,
    minHeight: 30,
  },
  labelContainer: {
    display: 'flex',
    alignItems: 'center',
  },
  iconContainer: {
    marginTop: 4,
    paddingRight: 8,
  }
});
